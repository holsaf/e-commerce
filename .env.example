# Environment Variables for Docker Compose (DEV)
# Copy this file to .env if you want to override defaults

# Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=dev

# JWT Configuration (optional override for dev)
JWT_SECRET=dev-jwt-secret-key-for-local-testing-change-in-production
JWT_EXPIRATION=86400000

# Notes for DEV environment:
# 1. Docker container will connect to MySQL on your host machine (127.0.0.1:3306)
# 2. Make sure MySQL is running locally with database 'virtual_shop'
# 3. Default credentials: root/adm123 (configured in application-dev.properties)
# 4. Access: http://localhost:8080/swagger-ui.html
